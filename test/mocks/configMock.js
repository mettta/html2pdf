// TODO

const defaultConfig = {
  debugMode: false,
  preloader: false,
  initialRoot: '#app',
};

const debugConfig = {
  debugMode: true,
  preloader: false,
  initialRoot: '#app',
};

const preloaderConfig = {
  debugMode: false,
  preloader: true,
  initialRoot: '#app',
};

export default {
  defaultConfig,
  debugConfig,
  preloaderConfig,
};
